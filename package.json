{
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "scripts": {
    "build": "yarn workspace capacitor-firebase-github-auth build",
    "ios": "yarn build && yarn workspace example ios",
    "android": "yarn build && yarn workspace example android"
  }
}
